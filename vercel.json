{
  "buildCommand": "npm run build:client",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "installCommand": "npm install --prefix client && npm install",
  "rewrites": [
    { "source": "/((?!data/|uploads/|assets/).*)", "destination": "/index.html" }
  ]
}
